{
  "name": "a-plus-skill",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/preflight.mjs",
    "pretest": "node scripts/preflight.mjs",
    "prebuild": "node scripts/preflight.mjs",
    "build": "tsc -p tsconfig.json",
    "dev": "node --import tsx src/index.ts",
    "report:send": "node --import tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": "22.22.0"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.7.3",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  }
}
